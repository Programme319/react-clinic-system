# Supabase — copy from Dashboard → Project Settings → API

# Project URL (must start with https:// and end with .supabase.co)
VITE_SUPABASE_URL=https://ifwvoyfyduyurwxwpzrj.supabase.co

# anon public key (starts with eyJ — NOT the service_role secret)
VITE_SUPABASE_ANON_KEY=sb_publishable_yQ8y935BLO3oqwkdLwkWag_JIxKTUkR

# Vercel: add both variables, then redeploy (env vars are baked in at build time)
